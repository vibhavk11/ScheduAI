query ScheduaiTasksById($input: String!) {
  scheduaiTasksById(id: $input) {
    id
    title
    description
    priority
    estimatedTimeInHours
    dueTime
    startTime
    endTime
    aiRecommendation
  }
}
